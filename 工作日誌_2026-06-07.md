# 工作日誌 2026-06-07

## 專案：美式口語互動對話卡 App (app_ai.html + GAS 後端)

### 完成項目

1. **GAS 後端部署**
   - 部署獨立的 GAS 後端（Code.gs）作為 API-only 服務
   - 提供 `action=getData`（GET）和 `action=saveData`（POST）端點
   - 使用 PropertiesService + SpreadsheetApp 儲存
   - 部署版本 @12，設定為「任何人」可存取
   - 清理舊版部署（v1–v11）

2. **前端獨立建置**
   - 發現 GAS HtmlService 不適合 React SPA（CSP 限制 + botguard 驗證）
   - 改為前端獨立托管，透過 GAS API 連線
   - 建立 `standalone/build_standalone.js` 建置腳本
   - 內嵌 React 18 UMD、ReactDOM UMD、Tailwind CSS、Lucide SVG 圖示
   - 生成 227KB 自包含 HTML，無任何外部依賴

3. **AI 功能整合**
   - 將 Gemini API 替換為 Groq API（OpenAI 相容格式）
   - 端點：`api.groq.com/openai/v1/chat/completions`
   - 模型：`llama-3.3-70b-versatile`
   - 保留 `dialogue_custom_api_key` localStorage key

4. **語音控制修復**
   - 修正 `useEffect` 依賴 `isListening` 造成的中斷問題（語音辨識初始化後被 cleanup 中止）
   - 改用空依賴陣列 + isListeningRef 避免閉包陳舊
   - 修正語音「播放」指令播放非當前卡片的問題（閉包捕獲舊 `speakCardText`）
   - 所有語音回呼改為透過 ref 存取最新函數
   - 無法匹配指令時自動送往 Groq AI 詢問

5. **語音指令強化**
   - 新增「方面」「反面」作為翻牌觸發關鍵字（解決語音辨識「翻面」→「方面」誤判）

6. **Bug 修復**
   - 修正 `useRef` 宣告位置造成的 Temporal Dead Zone 錯誤

### 關鍵檔案
- `/home/admin/english/gas_deploy/Code.gs` — GAS 後端（已部署）
- `/home/admin/english/standalone/app.html` — 自包含前端（227KB）
- `/home/admin/english/standalone/build_standalone.js` — 建置腳本

### GAS 端點
`https://script.google.com/macros/s/AKfycbwOBdzMMsA4hD43M4N8hP1bxf7XlKh9Odl6Oo1bqVSI60U8uJOZ2aVtNkX1jID-ZtXhIw/exec`
